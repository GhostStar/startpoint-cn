import { Card, Empty } from "antd"

// M3: 种子管理（对应旧 seeds.html + /api/seeds/*）
export default function Seeds() {
    return (
        <Card title="种子管理">
            <Empty description="M3 迁移中：统计、列表、模式切换、tag、测试种子" />
        </Card>
    )
}
